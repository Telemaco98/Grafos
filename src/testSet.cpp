#include <iostream>
using std::pair;
using std::cout;
using std::endl;

#include <list>
using std::iterator;

#include <set>
using std::set;

#include "Pair.h"

// int main() {
// 	set <Pair> setP;
// 	Pair* pair = new Pair(1000, 2);
// 	setP.insert(*pair);
// 	pair = new Pair(3000, 4);
// 	setP.insert(*pair);
// 	pair = new Pair(100, 6);
// 	setP.insert(*pair);

// 	Pair* searcher = new Pair(32, 4);

// 	for (auto it2 = setP.begin(); it2 != setP.end(); it2++) {
// 		Pair p = (*it2);
// 		if ((*searcher) == p) {

// 			cout << "Pair 	encontrado: " << p;
// 			break;
// 		}
// 	}

// }